<Page x:Class="workForm.Windows.Main.pgProjectList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:workForm.Windows.Main"
      mc:Ignorable="d" 
       Height="350" Width="500" 
      Title="pgProjectList">

    <Grid Height="280" VerticalAlignment="Bottom"  Margin="0,0,20,20">
        <ListView Name="lvProjects">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Customer" DisplayMemberBinding="{Binding CustomerNameString}" />
                    <GridViewColumn Header="Rate" DisplayMemberBinding="{Binding Rate}" />
                    <GridViewColumn Header="Completed" DisplayMemberBinding="{Binding Completed}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
